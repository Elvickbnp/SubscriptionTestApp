    <Инструкция по запуску приложения>
        1. В сборщике Maven запустить следующие команды:
            *package

        2. В консоли IDE запустить команду:
            * "docker-compose up --build"

    <Отправка запросов и проверка функционала>
        Все запросы соответствуют запросам указанным в тех.задании.

        Для того что бы отправить запрос вы можете воспользоваться приложением Postman.
        Для этого в строке запроса необходимо ввести url с содержанием "http://localhost:8080/<остальная часть запроса заполняется согласно запросам из ТЗ>

        В случае добавления пользователя в теле запроса необходимо указать передаваемые данные в виде:
            "{
                "name": "Test Name",
                "email": "test@example.com"
             }

        !ВАЖНО  После запуска приложения база данных будет пустая, поэтому для того чтобы совершать операции с пользователем или подписками их необходимо
                сначала создать, сделать это можно с помощью запросов.

        В случае добавления подписки необходимо так же написать запрос исходя из тех.задания и заполнить тело запроса по следующему примеру:
            "{
                "serviceName": "Test Service Name"
             }"

        Для всех остальных операций заполнять тело запроса не нужно, вся необходимая информация передается в самом запросе.